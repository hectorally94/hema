server.port=8088

spring.datasource.url=jdbc:mysql://localhost:3306/scomclinicOne?useSSL=false
spring.datasource.username=root
spring.datasource.password=210713@MONSter
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update

# Optional: Uncomment the following lines for additional configuration
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.globally_quoted_identifiers=true

# Optional: Uncomment the following line if needed
# spring.main.allow-circular-references=true


spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/quickstart
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/realms/quickstart/protocol/openid-connect/certs

jwt.auth.converter.resource-id=authz-servlet
jwt.auth.converter.principal-attribute=preferred_username
logging.level.org.springframework.security=DEBUG
